name: Build SegfaultHandler
description: Build SegfaultHandler node module

runs:
  using: "composite"
  steps:
    - run: sudo apt-get install libunwind-dev
      shell: bash
    - run: npm install
      shell: bash